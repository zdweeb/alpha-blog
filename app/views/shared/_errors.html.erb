<% if obj.errors.any? %>
  <div class="alert alert-danger" role="alert">
    <h4 class="title"><%= pluralize(obj.errors.count, "error") %> prevented this <%= "#{obj.class.name}" %> from being saved.;</h4>
  <div class="panel-body">
  <ul>
    <% obj.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <%end %>
  </ul>
  </div>
  </div>
<% end %> 